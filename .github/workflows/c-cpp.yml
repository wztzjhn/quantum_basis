name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-22.04
    
    env:
      LD_LIBRARY_PATH: '/home/runner/installs/lib'
      MKL_INC_DIR: '/usr/include/mkl'
      MKL_LIB_DIR: '/usr/lib/x86_64-linux-gnu'
      FFLAGS: '-m64 -I/usr/include/mkl'
      FCFLAGS: '-m64 -I/usr/include/mkl'
      CFLAGS: '-DMKL_ILP64 -m64 -I/usr/include/mkl'
      CXXFLAGS: '-DMKL_ILP64 -m64 -I/usr/include/mkl'
      LIBSUFFIX: 'ILP64'
      INTERFACE64: '1'
      OMP_NUM_THREADS: 2

    steps:
    - uses: actions/checkout@v3
    
    - name: Install dependencies
      run: |
        mkdir ~/installs
        mkdir ~/installs/lib
        mkdir ~/installs/include
        sudo apt-get update
        sudo apt-get install -y libmkl-dev libboost-dev
        cd /tmp
        wget https://github.com/wztzjhn/arpack-ng/archive/refs/heads/dev.zip
        unzip dev.zip
        cd arpack-ng-dev/
        sh bootstrap
        export LIBS='-L/usr/lib/x86_64-linux-gnu -Wl,--no-as-needed -lmkl_gf_ilp64 -lmkl_tbb_thread -lmkl_core -lpthread -lm -ldl'
        ./configure --enable-icb --with-blas=mkl_gf_ilp64 --with-lapack=mkl_gf_ilp64 --prefix=$HOME/installs
        make check
        make install
    
    - name: make test
      run: |
        echo " ------ $LD_LIBRARY_PATH ------"
        mkdir bin
        cd src; make test -j2
        cd ../bin; ./test.x
